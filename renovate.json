{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits"
  ],
  "timezone": "Europe/Prague",
  "schedule": [
    "every weekend"
  ],
  "labels": [
    "renovate"
  ],
  "argocd": {
    "fileMatch": [
      "kubernetes/bootstrap/apps/.+\\.yaml$"
    ]
  },
  "helm-values": {
    "fileMatch": [
      "kubernetes/.+/values\\.yaml$",
      "kubernetes/bootstrap/apps/.+\\.yaml$"
    ]
  },
  "kubernetes": {
    "fileMatch": [
      "kubernetes/.+\\.yaml$"
    ]
  },
  "customManagers": [
    {
      "description": "Longhorn is installed with kubectl apply, not Helm or ArgoCD, so no standard manager can see it. Track the version pinned in its README instead \u2014 this is the only reason it sat 5 minor versions behind.",
      "customType": "regex",
      "fileMatch": [
        "^kubernetes/infrastructure/longhorn/README\\.md$"
      ],
      "matchStrings": [
        "longhorn/longhorn/(?<currentValue>v[0-9]+\\.[0-9]+\\.[0-9]+)/deploy/longhorn\\.yaml"
      ],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "longhorn/longhorn"
    }
  ],
  "packageRules": [
    {
      "description": "Majors stay off \u2014 they need reading, not a PR queue.",
      "matchManagers": [
        "kubernetes",
        "helm-values"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "enabled": false
    },
    {
      "description": "DEFAULT: digest and patch bumps auto-merge. These are the redis/ollama-style updates that drown the important PRs.",
      "matchUpdateTypes": [
        "digest",
        "patch"
      ],
      "automerge": true,
      "automergeType": "pr",
      "platformAutomerge": true
    },
    {
      "description": "Remaining minors group into one weekend PR. Placed BEFORE the infra/db rules: Renovate gives later rules precedence, so infra/database minors fall through to their own groups instead of landing here.",
      "matchUpdateTypes": [
        "minor"
      ],
      "groupName": "app minor updates"
    },
    {
      "description": "Infrastructure NEVER auto-merges \u2014 a bad cert-manager or ingress bump locks you out of everything downstream, and ArgoCD auto-syncs whatever lands on main.",
      "matchPackageNames": [
        "/cert-manager/",
        "/kube-vip/",
        "/longhorn/",
        "/argo-cd/",
        "/argocd/",
        "/ingress-nginx/",
        "/cloudflared/",
        "/velero/",
        "/kube-prometheus-stack/",
        "/loki/",
        "/promtail/",
        "/descheduler/"
      ],
      "automerge": false,
      "groupName": "infrastructure (manual review)"
    },
    {
      "description": "Data stores never auto-merge \u2014 any image bump restarts the pod, and a restart mid-backup silently produces empty dumps (see the netpol pod-admission lag).",
      "matchPackageNames": [
        "/postgres/",
        "/mariadb/",
        "/mysql/",
        "/redis/",
        "/mongo/",
        "/minio/"
      ],
      "automerge": false,
      "groupName": "databases (manual review)"
    },
    {
      "description": "Vaultwarden is manual: it holds every credential plus the SOPS break-glass key. An unattended restart that goes wrong locks you out of the recovery path itself.",
      "matchPackageNames": [
        "/vaultwarden/"
      ],
      "automerge": false
    },
    {
      "matchPackageNames": [
        "cloudflare/cloudflared"
      ],
      "automerge": false
    }
  ]
}