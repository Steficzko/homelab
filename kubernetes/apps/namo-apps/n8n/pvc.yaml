apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-data-pvc
  namespace: namo-apps
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn   # explicit: two default StorageClasses exist in this cluster
  resources:
    requests:
      storage: 5Gi
