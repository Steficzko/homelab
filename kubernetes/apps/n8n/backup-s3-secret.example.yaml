# Real values live in backup-s3-secret.sops.yaml (generated by
# scripts/bootstrap-backup-s3-creds.sh). This documents the shape only.
apiVersion: v1
kind: Secret
metadata:
  name: backup-s3-secret
  namespace: n8n
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "REPLACE_ME"
  AWS_SECRET_ACCESS_KEY: "REPLACE_ME"
  AWS_ENDPOINTS: "http://192.168.1.100:9100"
