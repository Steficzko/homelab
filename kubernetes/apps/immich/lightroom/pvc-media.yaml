apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lightroom-media-pvc
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: immich-lightroom-media-pv
  resources:
    requests:
      storage: 500Gi
