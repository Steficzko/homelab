apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lightroom-raw-pvc
  namespace: immich
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: ""
  volumeName: immich-lightroom-raw-pv
  resources:
    requests:
      storage: 10Ti
