apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stefanos-encoded-ssd-pvc
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: immich-stefanos-encoded-pv
  resources:
    requests:
      storage: 100Gi
