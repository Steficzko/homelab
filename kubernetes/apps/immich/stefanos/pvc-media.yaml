apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stefanos-media-pvc
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: immich-stefanos-media-pv
  resources:
    requests:
      storage: 200Gi
