apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stefanos-library-pvc
  namespace: immich
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: ""
  volumeName: immich-stefanos-library-pv
  resources:
    requests:
      storage: 500Gi
