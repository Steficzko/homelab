apiVersion: apps/v1
kind: Deployment
metadata:
  name: open-webui
  namespace: ai
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: open-webui
  template:
    metadata:
      labels:
        app: open-webui
    spec:
      containers:
        - name: open-webui
          image: ghcr.io/open-webui/open-webui:main
          ports:
            - containerPort: 8080
          env:
            - name: OLLAMA_BASE_URLS
              value: ""
            - name: WEBUI_AUTH
              value: "true"
            # Built-in sentence-transformers for RAG embeddings (ADR-018)
            - name: RAG_EMBEDDING_ENGINE
              value: ""
            - name: RAG_OLLAMA_BASE_URL
              value: ""
            - name: OPENAI_API_BASE_URL
              value: "http://litellm.ai.svc.cluster.local:4000/v1"
            - name: OPENAI_API_KEY
              value: "dummy"
            - name: AUDIO_STT_ENGINE
              value: "openai"
            - name: AUDIO_STT_OPENAI_API_BASE_URL
              value: "http://litellm.ai.svc.cluster.local:4000/v1"
            - name: AUDIO_STT_OPENAI_API_KEY
              value: "dummy"
          resources:
            requests:
              memory: 500Mi
              cpu: "250m"
            limits:
              memory: 3Gi
          volumeMounts:
            - name: data
              mountPath: /app/backend/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: open-webui-pvc
