apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-upload-pvc
  namespace: immich-lightroom
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: immich-lightroom-upload-pv
  resources:
    requests:
      storage: 500Gi
