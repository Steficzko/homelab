# Reference only — real values live in secret.sops.yaml (ArgoCD excludes this file).
apiVersion: v1
kind: Secret
metadata:
  name: padle-secret
  namespace: padle
stringData:
  POSTGRES_USER: padle
  POSTGRES_PASSWORD: CHANGE-ME
  POSTGRES_DB: padle
  DATABASE_URL: postgresql://padle:CHANGE-ME@padle-postgres:5432/padle
  SECRET_KEY: CHANGE-ME
  ADMIN_PASSWORD: CHANGE-ME
  # optional: N8N_WEBHOOK_URL: https://n8n.../webhook/padle-booking
