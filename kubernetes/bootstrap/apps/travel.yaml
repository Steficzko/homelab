apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: travel
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Steficzko/homelab.git
    targetRevision: HEAD
    path: kubernetes/apps/travel
    directory:
      exclude: "{*.sops.yaml,secret-placeholder.yaml}"
  destination:
    server: https://kubernetes.default.svc
    namespace: travel
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
