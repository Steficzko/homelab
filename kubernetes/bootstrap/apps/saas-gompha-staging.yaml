apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: saas-gompha-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Steficzko/homelab.git
    targetRevision: HEAD
    path: kubernetes/apps/saas-gompha/staging
    directory:
      recurse: true
      exclude: '{*.sops.yaml,*placeholder*.yaml,*.example.yaml}'
  destination:
    server: https://kubernetes.default.svc
    namespace: saas-gompha-staging
  syncPolicy:
    automated:
      prune: false               # NEVER auto-delete the DB PVC (manifest-reviewer W1; matches licensing sibling)
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
