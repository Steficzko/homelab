# /etc/whisper-gpu.env — copy to LXC 112

WHISPER_MODEL=large-v3
WHISPER_DIARIZATION=true
HF_TOKEN=

# RX 6700 XT (gfx1031) — required for ROCm to recognise the GPU
HSA_OVERRIDE_GFX_VERSION=10.3.0

PORT=8000
